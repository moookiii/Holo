# Flareon ex 146/131

The picker entry `pokemon:sv08.5-146:holo` uses the finished Prismatic SIR
lighting model with independently authored Flareon maps. The supplied etching
master guides the curved inner cuts and flowing outer flame engraving. The
second photograph guides the subject boundary. The local clean front supplies
registration and printed text, never height.

The supplied silver microdiamond PNG is retained byte-for-byte in research.
Its ex section is registered to Flareon's shorter title; the remaining trim
aligns with the print. Individually traced jewels and connected crown shards
complete the secondary foil region. The character, feet, and tail have their
own fine relief. Printed text and rules are protected independently. Continuous
field normals are evaluated before blending material coverage, avoiding raised
cutout borders and the lower seam encountered on Umbreon.

Fine edge etching and ink-colored reflections use the same direct lighting
behavior as the approved Umbreon. There is no sparkle layer on the body.
Incision spacing, depth, hidden continuation and optical constants are visual
estimates from the supplied photos, not measured manufacturing geometry.

Regenerate with `python scripts/prismatic/flareon_maps.py`. Nine PNG maps,
the three source hashes, and the reference policy are recorded in
`146-holo-evidence.json`. Review overlays and WebGPU angle captures are under
`artifacts/flareon-146/`.
