{
  "name": "holo-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "assets": "node scripts/create-original.mjs",
    "capture": "node scripts/capture.mjs"
  },
  "dependencies": {
    "@tcgdex/sdk": "^2.9.0",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/three": "0.186.0",
    "playwright": "^1.58.0",
    "typescript": "7.0.2",
    "vite": "8.3.0"
  }
}
